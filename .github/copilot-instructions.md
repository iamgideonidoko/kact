# Copilot instructions

Read `AGENTS.md` before editing. Keep Kact command-first, preserve opt-in global shortcuts, avoid unnecessary dependencies, and run the checks appropriate to the changed code.

